# Entity Resolution - Python dependencies
#
# Install:
#   python -m venv .venv
#   source .venv/bin/activate        # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#
# The pipeline is designed so the CPU-only stages (prepare / index / candidates /
# evaluate) need only the CORE set. GPU and model packages are optional extras at
# the bottom - installing them is never required to run the pipeline.

# ---- core: required for prepare_data / build_indexes / generate_candidates ----
numpy>=1.24,<3
pandas>=2.0
PyYAML>=6.0

# ---- optional but recommended: progress bars ----
tqdm>=4.66

# ---- reporting: RSS logging during long HPC jobs ----
psutil>=5.9

# ---- faster/lighter intermediate storage (set io.prepared_format: parquet) ----
# pyarrow>=14.0

# ---------------------------------------------------------------------------
# Optional extras - only needed from milestone 2 onwards.
# ---------------------------------------------------------------------------
# Lexical pair features (token/ratio similarity):
# rapidfuzz>=3.6

# Gradient-boosted trees for the match classifier:
# scikit-learn>=1.3
# lightgbm>=4.0

# Multilingual embeddings + transformer stages. GPU is used automatically when
# CUDA is available; CPU works but is slow.
# torch>=2.1
# transformers>=4.40
# sentence-transformers>=2.7

# Dense retrieval index (only if FAISS beats exact search at this scale):
# faiss-cpu>=1.7  ; or faiss-gpu on a CUDA node
